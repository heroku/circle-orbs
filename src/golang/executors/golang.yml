docker:
  - image: circleci/golang:<< parameters.tag >>
parameters:
  tag:
    description: tag of the circleci/golang docker image to use (Defaults to 'latest')
    type: string
    default: latest
resource_class: medium
