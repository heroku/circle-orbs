docker:
  - image: circleci/golang:<< parameters.tag >>
parameters:
  tag:
    description: tag of the circleci/golang docker image to use (Defaults to 1.13)
    type: string
    default: "1.13"
resource_class: medium
