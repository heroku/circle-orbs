docker:
  - image: golangci/golangci-lint:<< parameters.tag >>
parameters:
  tag:
    description: tag of the `golangci/golangci-lint` docker image to use (Defaults to 'latest')
    type: string
    default: latest
resource_class: small
